// Copyright © Blockchain Luxembourg S.A. All rights reserved.

import Foundation

enum AccessibilityIdentifiers {
    enum QRScanner {
        enum AllowAccessScreen {
            static let prefix = "QRCodeScannerAllowAccessView."
            static let headerTitle = "\(prefix)headerTitle"
            static let headerDescription = "\(prefix)headerDescription"
            static let scannerList = "\(prefix)benefitsList"
            static let ctaButton = "\(prefix)ctaButton"
        }
    }
}
