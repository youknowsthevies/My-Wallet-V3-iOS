// Copyright © Blockchain Luxembourg S.A. All rights reserved.

/// Colors are now defined in `UIComponentsKit`. This exported import makes them available also by importing `PlatformUIKit` for backwards compatibility
@_exported import UIComponentsKit

// MARK: - Color Palette - App Layer

/// Typealias to use instead of `UIColor`, to avoid from being dependent on `UIKit`
public typealias Color = UIColor
